gresources = gnome.compile_resources(
    'gresources',
    'resources.gresource.xml',
    source_dir: ['.'],
    gresource_bundle: true,
    install: true,
    install_dir: get_option('datadir') / 'direnderer',
)
